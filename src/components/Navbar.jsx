import { useState } from "react";
import { close, logo, menu } from "../assets";
import { navLinks } from "../constants";

function Navbar() {
  // useState
  return (
    <nav className="w-full border flex py-6 justify-between items-center navbar">
      hello
    </nav>
  );
}

export default Navbar;
